name: PR Build Check

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read
  packages: read

jobs:
  build-fedora:
    name: Build Fedora image (verification)
    uses: ./.github/workflows/build-image.yml
    with:
      push: false
